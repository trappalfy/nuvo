node_modules/
.next/
out/
cache/
.env*.local
*.tsbuildinfo
next-env.d.ts

# Review screenshots and the raw generated clip: local only, both are heavy.
shots/
reference/
background-nuvo.mp4

# Local rehearsal runs. Real deployments keep their record.
contracts/broadcast/*/31337/

# Anything that will hold a key. The .example files stay: they hold no secrets.
.env
.env.*
!.env.example

# The devnet process id: local runtime, not history.
contracts/.devnet.pid
